name: Edit badges

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      env:
        env_name:
          type: string
          required: true
        vers:
          type: string
          required: true
        
permissions:
  contents: read

jobs:
  Edit_Badges:
    runs-on: ubuntu-latest
    steps:
      - name: Git clone the repository
        uses: actions/checkout@v4